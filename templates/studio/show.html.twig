{% extends 'base.html.twig' %}

{% block title %}show art studio {% endblock %}
{% block meta %}show art studio {% endblock %}

{% block body %}

    <h1>show art studios</h1>

    <p>Name: {{ studio.name}}</p>
    <p>Capacity:  {{ studio.nbRooms}}</p>

    <p>test:  {{ studio.nbRegistrationMade}}</p>

    {{dump(studioTimeslots)}}

    {# {% for studioTimeslot in studioTimeslots %}
        <p>timeslot:  {{ studioTimeslot.id}}  +  {{ studioTimeslot.studio}} -  {{ timeslotRegistrations[studioTimeslot.id] }} réservations</p>
    {% endfor %} #}
    

    <p>Plages horaires:</p>

    {{ dump(studio)}}

    {% if studio.timeslots|length > 0 %}
        <ul>
            {# Affichier date : 00-00-0000 puis créneau horaire 9h00-10h00) #}
            {% for timeslot in studio.timeslots %}
            <li>
                {{timeslot.startDate|formatDate}} -  {{timeslot.endDate|formatDate}}  | {{timeslot.user}} <p>timeslot:  {{ timeslotRegistrations[timeslot.id] }} registration/s /   {{timeslot.studio.nbRooms}} </p>
            </li>

            {% if timeslotRegistrations[timeslot.id] ==  timeslot.studio.nbRooms %}
                <p>plus de place disponibles</p>
            {% else %}
                
                <p>Réserver cette plage horaire:</p>
                <a href="{{ path('new_registration', {'id': timeslot.id}) }}">réserver </a>
                
            {% endif %}

                
            {% endfor %}
        </ul>
    {% else %}
            <p>Pas de créneau horaire dispo pour ce studio.</p>
    {% endif %}



    


{% endblock %}
