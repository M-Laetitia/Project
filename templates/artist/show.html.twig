{% extends 'base.html.twig' %}

{% block title %}Detail artist{% endblock %}

{% block body %}


    <h1>Detail artist</h1>

    {% for artistData in artist %}
        <p>ID: {{ artistData.id }}</p>
        <p>Artist Name: {{ artistData.artistInfos.artistName }}</p>
        {# {{ dump(artist)}} #}
        <p>Discipline: {{ artistData.artistInfos.discipline }}</p>
    {% endfor %}



    <div class="artist-works">
        <a href=""></a>
        <a href=""></a>
        <a href=""></a>
    </div>

    <div class="show-more-artist-work">
        <a href=""></a>
        <a href=""></a>
        <a href=""></a>
    </div>

   
{% endblock %}
